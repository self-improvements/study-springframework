rootProject.name = 'study-springframework'

// Includes subprojects in root directory as modules automatically.
file(".").listFiles({ it.isDirectory() && it.name.startsWith('spring-') } as FileFilter).each {
    include ":${it.name}"
}
